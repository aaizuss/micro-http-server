package com.aaizuss;

import com.aaizuss.http.Status;
import com.aaizuss.mock.MockHandler;
import org.junit.Before;
import org.junit.Test;

import java.io.IOException;

// Server isn't testable unless I add a constructor that takes a SocketService, which I don't want to do
// because I don't want users to deal with sockets

public class ServerTest {

//    private Server server;
//    private Router router;
//
//    @Before
//    public void setUp() {
//        router.addRoute("GET", "/", new MockHandler(Status.OK));
//        router.addRoute("DELETE", "/", new MockHandler(Status.METHOD_NOT_ALLOWED));
//        router.addRoute("GET", "/coffee", new MockHandler(Status.COFFEE_POT));
//        server = new Server(router);
//    }
//
//    @Test
//    public void testRun() throws IOException {
//
//    }

}
